import { Sidebar, Chat } from '../components';

const Home = () => {
    return (
        <main className='container-lg'>
            <Sidebar />
            <Chat />
        </main>
    );
};

export default Home;
